<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Editar Usuario</title>
  <style>
    .edit-container {
      width: 300px;
      margin: 0 auto;
      padding: 20px;
      background-color: #f0f0f0;
      border: 1px solid #ccc;
      border-radius: 5px;
      text-align: center;
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
    }
    .form-label {
      display: block;
      margin-bottom: 5px;
      font-weight: bold;
    }
    .form-input {
      width: 100%;
      padding: 5px;
      margin-bottom: 15px;
      border: 1px solid #ccc;
      border-radius: 3px;
    }
    .form-button {
      background: linear-gradient(to bottom, #007BFF 0%, #0056b3 100%);
      color: #fff;
      padding: 10px 20px;
      border: none;
      border-radius: 3px;
      cursor: pointer;
      transition: background 0.3s;
    }
    .form-button:hover {
      background: linear-gradient(to bottom, #0056b3 0%, #004294 100%);
    }
    .back-link {
      display: block;
      margin-top: 20px;
      text-decoration: none;
      color: #007BFF;
      font-weight: bold;
    }
  </style>
</head>
<body>
  <div class="edit-container">
    <h1>Editar Usuario</h1>
    <form action="/users/update/<%= user._id %>" method="POST">
      <label for="name" class="form-label">Nombre:</label>
      <input type="text" id="name" name="name" value="<%= user.name %>" required class="form-input">
      <label for="email" class="form-label">Email:</label>
      <input type="email" id="email" name="email" value="<%= user.email %>" required class="form-input">
      <label for="password" class="form-label">Contraseña:</label>
      <input type="password" id="password" name="password" value="<%= user.password %>" placeholder="Dejar en blanco para mantener la misma contraseña" class="form-input">
      <button type="submit" class="form-button">Actualizar Usuario</button>
    </form>
    <a href="/users" class="back-link">Regresar a la lista de usuarios</a>
  </div>
</body>
</html>
